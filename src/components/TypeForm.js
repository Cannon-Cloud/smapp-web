import { Popover } from "@typeform/embed-react";

const TypeForm = () => {
  return (
    <Popover
      id="oqyzUbgk"
      tooltip="Interested in our product?<br/>Click here"
    />
  );
};

export default TypeForm;
