import { PopupButton } from "@typeform/embed-react";

const TypeFormContat = () => {
  return (
    <PopupButton
      id="oqyzUbgk"
      style={{ fontSize: 25 }}
      className="btn btn-custom theme-color"
    >
      Click Here To Submit
    </PopupButton>
  );
};

export default TypeFormContat;
